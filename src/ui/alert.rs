//! Placeholder for Task 5 of the G'MIC picker plan: `NSAlert`-based
//! error-sink with a trait so tests can substitute a capture impl.
//!
//! Wave 2 implementer (subagent T5) replaces this entire file.
//! Pre-stubbed so the parallel Wave-2 worktrees don't conflict on
//! `src/ui/mod.rs`'s module declaration block.
