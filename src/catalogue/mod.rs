//! Placeholder for Task 2 of the G'MIC picker plan: catalogue data
//! types, `#@gui` parser, reconciliation, and `builtin()`.
//!
//! Wave 2 implementer (subagent T2) replaces this entire file and
//! adds sibling `parser.rs` + `reconcile.rs`. Pre-stubbed here so all
//! four Wave-2 worktrees can safely share `src/lib.rs`'s module
//! declaration block without conflicting.
